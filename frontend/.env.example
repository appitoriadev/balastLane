# Copy this file to .env and adjust as needed.
# When using the Vite dev server proxy (default), VITE_API_URL is not required.
# Set it only if you are hitting the backend directly (e.g. in production).
VITE_API_URL=
